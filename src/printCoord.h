#ifndef PRINTCOORD_H
#define PRINTCOORD_H
#include <cstdlib>
#include <iostream>
#include <math.h>
#include <string>

bool Coord(float& x, float& y, float& r);
bool check(std::string str, std::string& strC, int i, int o);

#endif
